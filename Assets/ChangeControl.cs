using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.InputSystem;

public class ChangeControl : MonoBehaviour
{/**
    [SerializeField] private InputActionReference action1;
    [SerializeField] private InputActionReference action2;
    [SerializeField] private InputActionReference action3;
    [SerializeField] private InputActionReference action4;

    public void ClicSouris()
    {
        InputBinding binding = triggerAction.action.bindings[7];
        binding.overridePath = "<Mouse>/leftButton";
        triggerAction.action.ApplyBindingOverride(0, binding);

        binding = triggerAction.action.bindings[8];
        binding.overridePath = "<Mouse>/rightButton";
        triggerAction.action.ApplyBindingOverride(0, binding);

        binding = triggerAction.action.bindings[9];
        binding.overridePath = "<Keyboard>/#(q)";
        triggerAction.action.ApplyBindingOverride(0, binding);

        binding = triggerAction.action.bindings[10];
        binding.overridePath = "<Keyboard>/#(e)";
        triggerAction.action.ApplyBindingOverride(0, binding);
    }

    public void ClicClavierr()
    {
        InputBinding binding = triggerAction.action.bindings[7];
        binding.overridePath = "<Keyboard>/#(1)";
        triggerAction.action.ApplyBindingOverride(0, binding);

        binding = triggerAction.action.bindings[8];
        binding.overridePath = "<Keyboard>/#(2)";
        triggerAction.action.ApplyBindingOverride(0, binding);

        binding = triggerAction.action.bindings[9];
        binding.overridePath = "<Keyboard>/#(3)";
        triggerAction.action.ApplyBindingOverride(0, binding);

        binding = triggerAction.action.bindings[10];
        binding.overridePath = "<Keyboard>/#(4)";
        triggerAction.action.ApplyBindingOverride(0, binding);
    }**/
}
